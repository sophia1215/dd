<% provide(:title, "重寄確認信") %>

<div class="col-md-4 col-md-offset-4">
  <div class="confirmation-panel panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">重寄確認信</h3>
    </div>
    <div class="panel-body">
      <%= simple_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
        <%#= f.error_notification %>
        <div id="error_explanation" class="col-md-12">
          <%= devise_error_messages! %>
        </div>
        <%= f.full_error :confirmation_token %>

        <div class="col-md-12">
          <div class="form-group has-feedback">
            <%= f.input :email, :label=>false, required: true, autofocus: true, placeholder: "sophia@example.com" %>
            <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
          </div>

          <div class="form-actions">
            <%= f.submit "重寄確認信", class: "btn btn-large btn-primary btn-block" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div>
    <%= render "users/shared/links" %>
  </div>
</div>